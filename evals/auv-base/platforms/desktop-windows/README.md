# Windows desktop evaluations

NOTICE: No evaluation tasks are implemented here yet. This directory reserves
the platform boundary requested by the owner. Add `tasks/`, `cases/`, and native
`test-objects/` when a Windows evaluation slice is approved; shared desktop
receivers belong in `../desktop-universal/test-objects/`.
