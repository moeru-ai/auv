interface:
  display_name: "Create PR"
  short_description: "Disclose API changes and migration steps in PRs"
  default_prompt: "Use $create-pr to prepare a PR with API changes, before-and-after examples, and migration notes."
