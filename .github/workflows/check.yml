name: Check

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  rust:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-24.04
          - macos-14
          - windows-2025
    runs-on: ${{ matrix.os }}
    name: rust checks ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libclang-dev \
            libxcb1-dev \
            libxrandr-dev \
            libdbus-1-dev \
            libpipewire-0.3-dev \
            libwayland-dev \
            libegl-dev \
            libleptonica-dev \
            libtesseract-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --check
      - run: cargo check
      - run: cargo test
      - run: cargo run --quiet -- invoke --help
      - run: bash scripts/ci/public-claims-check.sh
      - name: AX path parse characterization
        if: runner.os == 'macOS'
        run: bash scripts/ci/ax-path-characterization.sh
      # TODO(windows-install-smoke): add a PowerShell smoke test with `.exe`
      # path handling when the project claims Windows installation support.
      - name: Install smoke test
        if: runner.os != 'Windows'
        run: bash scripts/ci/install-smoke.sh
      - run: git diff --check
